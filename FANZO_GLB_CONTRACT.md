# FANZO GLB CONTRACT

One path. Same loader everywhere.

```
/glbs/blue-speedster.glb
```

Source: Meshy_AI_Blue_Speedster_0922124133_texture.glb
Loader: Three GLTFLoader / drei useGLTF via CityGLB (clone, Box3 normalize, targetSize, gold glow).
Fallback: ProceduralSpeedsterMesh if the GLB is missing.
